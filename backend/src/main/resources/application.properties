server.port=8080

spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:54371/clearing}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:clearing}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:clearing123}
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.open-in-view=false
spring.jpa.properties.hibernate.jdbc.time_zone=UTC
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

app.jwt.secret=${JWT_SECRET:clearing-netting-demo-secret-key-32bytes!!}
app.jwt.expiration-ms=86400000

# Pacing between persisted run phases (VALIDATING/CALCULATING/PERSISTING) so a
# polling UI can observe progress. Set to 0 to run as fast as possible.
app.netting.stage-delay-ms=${NETTING_STAGE_DELAY_MS:600}

logging.level.com.clearing=INFO
